prompt = """
Analyze the technical debt for the repository at: {{args}}

Use the scan_repo tool with { "repoPath": "{{args}}" }

The scan will automatically save a markdown report to the repository folder.
Provide a summary of the key findings from the scan results.
"""
