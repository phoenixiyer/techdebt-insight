prompt = """
Generate a comprehensive CTO-level technical debt report for the repository at: {{args}}

Execute these tools in order:
1. scan_repo with { "repoPath": "{{args}}" }
2. dep_audit with { "repoPath": "{{args}}" }
3. generate_report with { "scanResults": "<scan_json>", "auditResults": "<audit_json>", "repoPath": "{{args}}", "projectName": "<project_name>" }

The report will be saved as:
- Markdown: techdebt-cto-report-YYYY-MM-DD.md
- PDF: techdebt-cto-report-YYYY-MM-DD.pdf (professional executive format)

Both files will be saved in the scanned repository directory.
Provide an executive summary of the key findings.
"""
